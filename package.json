{
  "name": "it-blog-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "build": "npm run build --workspace @it-blog/shared && npm run build --workspace @it-blog/api && npm run build --workspace @it-blog/web",
    "lint": "npm run lint --workspace @it-blog/web && npm run lint --workspace @it-blog/api",
    "typecheck": "npm run typecheck --workspace @it-blog/shared && npm run typecheck --workspace @it-blog/api && npm run typecheck --workspace @it-blog/web"
  }
}
