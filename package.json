{
  "name": "it-blog-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "build:api": "npm run build --workspace=@it-blog/shared && npm run build --workspace=@it-blog/api && echo '=== STRUCTURE AFTER BUILD ===' && find apps/api/dist -type f | head -20",
    "start:api": "node apps/api/dist/apps/api/src/index.js",
    "build:web": "npm run build --workspace=@it-blog/shared && npm run build --workspace=@it-blog/web",
    "start:web": "npm run start --workspace=@it-blog/web"
  }
}
